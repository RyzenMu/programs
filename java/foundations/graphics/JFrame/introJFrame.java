import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class introJFrame {
    
    public static void main(String args[]){
        // JFrame frame = new JFrame(null, null); // creates a frame
        // frame.setSize(420, 420); // sets the size of the frame
        // frame.setTitle("JFrame title goes hereS");
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit on close
        // frame.setResizable(false);// prevent resizing of frame

        // frame.setVisible(true);

        // ImageIcon image = new ImageIcon("girl.jpg");
        // frame.setIconImage(image.getImage()); // change icon of frame

        // //change background color of the frame
        // frame.getContentPane().setBackground(Color.BLUE);
        // frame.getContentPane().setBackground(new Color(255, 155, 55, 100));
        // frame.getContentPane().setBackground(new Color(0x349aca));

        JFrameClass myframe = new JFrameClass();


    }
}

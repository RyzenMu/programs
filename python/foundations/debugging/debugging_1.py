import pdb

pdb.set_trace()

# import pdb and pdb.set_trace() is used for setting the python debugger

name = str(input('Enter your name'))

age = int(input('Enter your age'))

total = name + age

# l is used for the line in the code

# n is used for the next line in the code.

